{% extends "base.html.twig" %}

{% block title %}Mon espace - ThePiston{% endblock %}
{% block main_class %}admin-dashboard{% endblock %}

{% block extra_head %}
    <meta name="robots" content="noindex, nofollow">
{% endblock %}


{% block content %}
    {% if session['user_role'] == 'admin' %}
        <h1 class="main-title">Mon espace Administrateur</h1>
    {% else %}
        <h1 class="main-title">Mon espace Pilote</h1>
    {% endif %}

    <section class="dashboards-section">
        <h2 class="dashboard-title">Les comptes élèves</h2>

        <div class="search-container">
            <form action="/dashboard/admin" method="GET" class="search-bar-admin">
                <input type="hidden" name="surnameP" value="{{ surnameP }}">
                <input type="hidden" name="nameP" value="{{ nameP }}">
                <input type="hidden" name="pageP" value="{{ current_pageP }}">
                <input type="hidden" name="page" value="1">
                <input class="search-item" type="text" name="surname" value="{{surname}}" placeholder="Nom">
                <input class="search-item" type="text" name="name" value="{{name}}" placeholder="Prenom">
                <input class="search-item" type="text" name="promotion" value="{{promotion}}" placeholder="Promotion">
                <button type="submit" class="search-btn" aria-label="Rechercher">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>
                <a href="/dashboard/admin/create-student" type="button" class="search-item btn-add add-link">+</a>    
            </form>
                      
        </div>

        <div class="box-list">
            {% for student in students %}
            <a href="/dashboard/admin/student-details?id={{ student.ID_utilisateur }}" class="link">
            <table class="company-table admin">
                <tr>
                    <td class="user-name">{{ student.Nom }} {{ student.Prenom }} </td>
                    <td class="user-promotion">{{ student.Promotion }}</td>
                    <td class="stats"><strong>Nombre de candidatures : {{ student.nb_candidature }}</strong></td>
                    <td class="btn-table">
                        <a href="/dashboard/admin/update-student?id={{ student.ID_utilisateur }}" 
                            class="btn-action crayon">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                        {# Lien de suppression avec l'ID dynamique #}
                        <a href="/dashboard/admin/delete-student?id={{ student.ID_utilisateur }}" 
                            class="btn-action poubelle" 
                            onclick="return confirm('Supprimer cet étudiant ?')">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </td>
                </tr>
            </table>
            </a>
            {% endfor %}
        </div>

        {# Système de pagination avec tes classes CSS #}
        <div class="pagination">
            {% if total_pages > 1 %}
                {% for i in 1..total_pages %}
                    <a href="/dashboard/admin?surname={{ surname }}&name={{ name }}&promotion={{ promotion }}&surnameP={{ surnameP }}&nameP={{ nameP }}&page={{ i }}&pageP={{ current_pageP }}" 
                    class="{{ i == current_page ? 'active' : '' }}">
                        {{ i }}
                    </a>
                {% endfor %}
            {% endif %}
        </div>
    </section>

{% if (session['user_role'] == 'admin') %}
    <section class="dashboards-section">
        <h2 class="dashboard-title">Les comptes pilotes</h2>

        <div class="search-container">
            <form action="/dashboard/admin" method="GET" class="search-bar-admin">
                <input type="hidden" name="surname" value="{{ surname }}">
                <input type="hidden" name="name" value="{{ name }}">
                <input type="hidden" name="promotion" value="{{ promotion }}">
                <input type="hidden" name="page" value="{{ current_page }}">
                <input type="hidden" name="pageP" value="1">
                <input class="search-item" type="text" name="surnameP" value="{{ surnameP }}" placeholder="Nom">
                <input class="search-item" type="text" name="nameP" value="{{ nameP }}" placeholder="Prenom">
                <button type="submit" class="search-btn" aria-label="Rechercher">
                    <i class="fa-solid fa-magnifying-glass"></i>
                </button>
                <a href="/dashboard/admin/create-pilot" type="button" class="btn-add add-link">+</a>
            </form>
        </div>

        <div class="box-list">
            {% for pilot in pilots %}
            <table class="company-table admin">
                <tr>
                    <td class="user-name">{{ pilot.Nom }} {{ pilot.Prenom }}</td>
                    <td class="user-stats">{{ pilot.Email }}</td>
                    <td class="btn-table">
                        <a href="/dashboard/admin/update-pilot?id={{ pilot.ID_utilisateur }}" 
                            class="btn-action crayon">
                            <i class="fa-solid fa-pen-to-square"></i>
                        </a>
                        {# Lien de suppression avec l'ID dynamique #}
                        <a href="/dashboard/admin/delete-pilot?id={{ pilot.ID_utilisateur }}" 
                            class="btn-action poubelle" 
                            onclick="return confirm('Supprimer ce pilote ?')">
                            <i class="fa-solid fa-trash"></i>
                        </a>
                    </td>
                </tr>
            </table>
            {% endfor %}
        </div>

        <div class="pagination">
            {% if total_pagesP > 1 %}
                {% for i in 1..total_pagesP %}
                    <a href="/dashboard/admin?surname={{ surname }}&name={{ name }}&promotion={{ promotion }}&surnameP={{ surnameP }}&nameP={{ nameP }}&page={{ current_page }}&pageP={{ i }}" 
                    class="{{ i == current_pageP ? 'active' : '' }}">
                        {{ i }}
                    </a>
                {% endfor %}
            {% endif %}
        </div>
    </section>
{% endif %}
{% endblock %}
