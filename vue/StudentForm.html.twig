{% extends "base.html.twig" %}

{% block title %}{{ is_edit ? 'Modifier' : 'Ajouter' }} un étudiant - ThePiston{% endblock %}

{% block main_class %}list-page{% endblock %}
{% block content %}
<section class="frame">
    <h1 class="main-title">{{ is_edit ? 'Modifier' : 'Ajouter' }} un étudiant</h1>

    <form class="form-test" data-account-password-form="true" data-popup-error="{{ popup_error|default('')|e('html_attr') }}" action="/{{ is_edit ? 'dashboard/admin/update-student&id=' ~ etudiant.ID_utilisateur : 'dashboard/admin/create-student' }}" method="POST">
        {% if session.user_role == 'admin' %}
            <label for="id_pilote">Sélectionner un pilote référent :</label>
            <select class="form-input" name="id_pilote" id="id_pilote" required>
                <option value="">-- Choisir un pilote --</option>
                {% for p in pilotes %}
                    <option value="{{ p.ID_utilisateur }}" 
                        {% if etudiant is defined and etudiant.ID_pilote == p.ID_utilisateur %}selected{% endif %}>
                        {{ p.Nom|upper }} {{ p.Prenom }}
                    </option>
                {% endfor %}
            </select>
        {% endif %}
        
        <label>Nom</label>
        <input class="form-input" type="text" name="surname" value="{{ etudiant.Nom ?? '' }}" required>

        <label>Prénom</label>
        <input class="form-input" type="text" name="name" value="{{ etudiant.Prenom ?? '' }}" required>

        <label>Promotion</label>
        <input class="form-input" type="text" name="promotion" value="{{ etudiant.Promotion ?? '' }}" required>

        <label>E-mail</label>
        <input class="form-input" type="email" name="email" value="{{ etudiant.Email ?? '' }}" required>

        <label>Mot de passe</label>
        <input class="form-input" id="password" type="password" name="password" {% if is_edit %} placeholder="Laissez vide pour ne pas modifier"{% endif %} {% if not is_edit %}required{% endif %}>

        <label>Confirmation du mot de passe</label>
        <input class="form-input" id="confirm_password" type="password" name="confirm_password" {% if not is_edit %}required{% endif %}>

        <button class="btn" type="submit">Valider</button>
        <a href="/dashboard/admin" class="btn-annuler">Annuler</a>
    </form>
</section>
{% endblock %}

{% block scripts %}
<script src="/assets/js/password-confirmation.js" defer></script>
{% endblock %}